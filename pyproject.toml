[project]
name = "code2vec"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.3",
    "tensorflow>=2.0.0b1",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

